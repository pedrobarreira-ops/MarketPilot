# Epic 2 Retrospective — API Key Security Layer

**Date:** 2026-04-18
**Epic:** 2 — API Key Security Layer
**Facilitator:** Bob (Scrum Master)
**Participants:** Pedro (Project Lead / sole developer), Alice (Product Owner), Amelia (Developer), Quinn (QA Engineer), Winston (Architect)
**Status:** Complete

---

## Epic Summary

**Delivery:**
- Completed: 2/2 stories (100%)
- Stories: 2.1 keyStore module → 2.2 BullMQ Worker scaffold + key lifecycle
- ATDD tests: 40 total (27 in 2.1, 13 in 2.2) — all green
- Code review patches applied: 4 (1 in Story 2.1, 3 in Story 2.2)
- Deferred technical debt items: 5

**Quality:**
- Security constraint violations: 0 (api_key never in queue payload, logs, or DB)
- Production incidents: 0
- Deployment: Live on Coolify, auto-deploying on every push to main

---

## What Went Well

1. **BAD pipeline autonomy exceeded expectations.** Pedro barely intervened — implementation matched spec almost exactly, tests passed first run, code review caught real issues without requiring Pedro to have deep technical knowledge to evaluate them. Confidence earned to run BAD more autonomously in Epic 3.

2. **Security architecture held perfectly — again.** Zero violations for the second epic running. `api_key` never appeared in queue payload, logs, or any DB column. The keyStore boundary worked exactly as designed.

3. **ATDD-first approach continued to deliver.** Pre-written test files were the contract — implementation had a fixed target. No guesswork about what "done" means.

4. **Code review caught real issues organically.** Story 2.2 review: (a) `NODE_ENV === 'test'` worker gate fixed to add fail-loud warning — would have silently disabled jobs if accidentally set in production; (b) `fastify.log.error(err)` full-object log in `server.js` replaced with safe fields — this closed an Epic 1 deferred debt item without a dedicated story.

5. **Epic 1 action items: 4/4 completed.** Multi-pass reviews (✅), story file status tracking (✅), `npm audit` fixes (✅), Coolify deployment (✅ — live and auto-deploying, just not formally tracked).

6. **This retro closed two critical path items during the session itself.** MCP verification of OF21, P11, and PRI01 was completed live; epics-distillate.md updated with MCP-Verified Endpoint Reference (2026-04-18). Process working as designed.

---

## Challenges

1. **Story spec file not committed by BAD's `create-story` step.** Story 2.2 spec existed as an untracked file locally and was committed manually post-merge. No audit trail of the contract driving implementation until after the fact. Risk multiplies across seven Epic 3 stories if not fixed.

2. **Completed critical path items invisible to the process.** Coolify deployment was done and working, but never tracked in sprint-status or any artifact. Appeared as an open blocker until Pedro clarified verbally in this retro.

3. **Repeated deferral of keyStore `set()` input validation.** Falsy/null apiKey guard deferred from Story 2.1 and again from Story 2.2 — both times correctly (guard belongs in Story 4.1). Third deferral would be a pattern worth investigating.

---

## Previous Epic 1 Retrospective — Follow-Through

| Action Item | Status | Notes |
|---|---|---|
| Budget 2 code review passes per story | ✅ Completed | Both stories had full review passes; Story 2.2 had auto-patches + post-merge manual pass |
| Story file Status header must match sprint-status at close | ✅ Completed | Both story files closed correctly |
| Run `npm audit` | ✅ Completed | sprint-status last_updated: "npm audit fixes applied" |
| Coolify deployment before Epic 2 | ✅ Completed | Live and auto-deploying; not formally tracked but verified in this retro |

**Result: 4/4 committed items completed.**

---

## Key Insights

1. **BAD pipeline autonomy is proven** — Epic 2 ran almost without Pedro's intervention; confidence to scale that trust into Epic 3's seven stories is earned, not assumed.
2. **Security architecture holds under autonomous execution** — zero violations for the second epic running; the keyStore boundary worked exactly as designed.
3. **Process gaps need hard gates, not guidelines** — story spec commit and MCP verification both promoted from soft rules to explicit pipeline steps; soft rules don't reliably execute at 2am.
4. **Boring reliability compounds** — 4/4 Epic 1 action items completed; the system is building trust incrementally, not through heroics.

---

## Epic 3 Preview: Report Generation Pipeline

**Stories:** 3.1 Mirakl API client+retry → 3.2 OF21 catalog fetch → 3.3 P11 competitor scan → 3.4 WOW+Quick Wins scoring → 3.5 Report persistence+CSV → 3.6 Email dispatch → 3.7 Full worker orchestration

**Dependencies on Epic 2:**
- `reportWorker.js` phase stubs A–E ready to be filled in — ✅ solid scaffold
- `keyStore.js` in place and tested — ✅

**Preparation context:**
- MCP-Verified Endpoint Reference added to epics-distillate.md today — OF21, P11, PRI01 confirmed including PRI01 rate limit behaviour and delete-and-replace mode
- Real Worten API key live; scripts (`scale_test.js`, `opportunity_report.js`) tested against real API at 31,000 products — patterns confirmed
- No Epic 3 ATDD test files committed yet — this is the highest-risk gap; BAD Epic-Start must generate and commit them before Story 3.1 dev begins

---

## Action Items

### Process Improvements

1. **BAD pipeline must commit story spec file as part of `create-story` step**
   - Owner: Pedro (BAD config / coordinator prompt edit)
   - Deadline: Before Story 3.1 runs
   - Success criteria: Every Epic 3 story spec file appears in the branch commit from `create-story`, never as an untracked file at merge time

2. **Completed critical path items must be tracked in sprint-status (or equivalent) even when completed outside the formal story workflow**
   - Owner: Pedro
   - Deadline: Ongoing from Epic 3
   - Success criteria: No "done but untracked" items appear in Epic 3 retro

### Technical Debt

1. **Story 4.1 must explicitly close the keyStore `set()` falsy apiKey guard — not defer again**
   - Owner: Pedro (Story 4.1 spec must include this as a required AC, not just a deferred-work.md reference)
   - Priority: High — deferred twice; third deferral is a pattern
   - Success criteria: Story 4.1 spec contains explicit AC for input validation on the `set()` call path

---

## Epic 3 Preparation Tasks

### Critical — Must complete before Story 3.1 dev starts

- [x] **MCP verification of Mirakl endpoints** ✅ Done 2026-04-18
  - OF21, P11, PRI01 verified against live MCP
  - Two new findings documented: PRI01 rate limit behaviour, delete-and-replace mode
  - epics-distillate.md updated with MCP-Verified Endpoint Reference section

- [ ] **BAD `create-story` config: add spec file commit step**
  - Owner: Pedro
  - Action: Edit BAD coordinator/SKILL.md so `create-story` commits the story spec file before handing off to `dev-story`

- [ ] **BAD `create-story` config: add distillate endpoint gate**
  - Owner: Pedro
  - Action: Add explicit step — "read MCP-Verified Endpoint Reference in epics-distillate.md; confirm this story's endpoint usage matches it exactly; document confirmation in story spec"
  - Replaces live MCP call per story with verified distillate reference (token-efficient, works at 2am)

- [ ] **BAD Epic-Start: generate and commit ATDD test files for Epic 3**
  - Owner: BAD pipeline (Pedro reviews at HALT checkpoint)
  - Gate: Pedro must approve test quality at HALT before [C] — verify pagination boundaries, P11 partial failure handling, scoring edge cases covered; shallow tests get rejected and improved before dev starts

### Parallel — Can happen during early stories

- [ ] **Story 4.1 spec: include keyStore `set()` input validation as explicit AC**
  - Owner: Pedro (when create-story runs for Story 4.1)

---

## Critical Path — Before Story 3.1 Dev Runs

| Item | Status |
|---|---|
| MCP endpoint verification + distillate update | ✅ Done 2026-04-18 |
| BAD `create-story` config: spec file commit step | ⏳ Pending — Pedro to complete before BAD runs |
| BAD `create-story` config: distillate endpoint gate | ⏳ Pending — Pedro to complete before BAD runs |
| BAD Epic-Start: ATDD tests generated and committed | ⏳ Pending — runs as part of Epic-Start |
| Pedro reviews test quality at HALT checkpoint | ⏳ Pending — Pedro approves before [C] |

**Pedro's commitment:** Both create-story config edits will be made before BAD is told to run. Non-negotiable.

---

## Readiness Assessment

| Area | Status |
|---|---|
| Testing & Quality | ✅ 40/40 ATDD tests green, full suite passes |
| Deployment | ✅ Live on Coolify, auto-deploys on push to main |
| Stakeholder Acceptance | ✅ Pedro satisfied |
| Technical Health | ✅ Clean, two well-tested files, no production incidents |
| Unresolved Blockers | ⚠️ BAD config updates needed before Story 3.1 (not blocking epic close) |

**Verdict:** Epic 2 is complete — not just story-complete, actually done. Two of five critical path items for Epic 3 already cleared during this retro.

---

## Next Steps

1. **Pedro makes both BAD `create-story` config edits** — spec commit step + distillate endpoint gate
2. **BAD runs Epic-Start** — generates Epic 3 ATDD tests and commits them
3. **Pedro reviews test quality at HALT checkpoint** — approves or iterates before [C]
4. **Story 3.1 dev begins** — all gates cleared

---

Bob (Scrum Master): "A retro that closed two critical path items during the session itself. Meeting adjourned."

Alice (Product Owner): "Seven stories with real money implications. The gates aren't overhead — they're insurance."

Winston (Architect): "Epic 3 is where the product actually exists. We're ready."
